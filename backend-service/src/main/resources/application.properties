spring.application.name=resumebackend

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/resume_classifier_db
spring.datasource.username=postgres
spring.datasource.password=irfan1512

# Hibernate Settings (This creates the table automatically for you!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# File Size Limits (Keep these from our previous fix)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

